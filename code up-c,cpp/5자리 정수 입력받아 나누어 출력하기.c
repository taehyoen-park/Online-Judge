#include<stdio.h>

int main()
{
    int x;
    scanf("%d",&x);
    printf("[%d]\n[%d]\n[%d]\n[%d]\n[%d]",(x / 10000)*10000, (x-(x / 10000) * 10000)/1000*1000,(x-(x - (x / 10000) * 10000) / 1000 * 1000- (x / 10000) * 10000)/100*100, (x % 100 - x % 10), x % 10);
    return 0;
}
